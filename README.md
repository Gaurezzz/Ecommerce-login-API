# Comandos

```cpp
docker-compose up --build
docker exec -it ecommerce-login-app-1 bash
mysql -u admin -p
//ingresa la contraseña
docker exec -it ecommerce-login-bd-1 bash
uvicorn src.main:app --reload --host 0.0.0.0
```
